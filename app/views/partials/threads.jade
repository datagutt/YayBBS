extend ../layout
block content
	for thread in threads
		article.thread
			b 
				a(href='/thread/#{thread.id}/#{slug(thread.subject)}') #{thread.subject}
			span  by #{thread.user_id} on #{thread.created}